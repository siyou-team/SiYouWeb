//账户充值 qianyistore v 5 .3$(function() {	var e = getLocalStorage("ukey");	if (!e) {		window.location.href = WapSiteUrl + "/tmpl/member/login.html";		return ;	}	$.sValid.init({		rules: {			pdr_amount:"required"		},		messages: {			pdr_amount:"请输入充值金额！"		},		callback: function(e, r, a) {			if (e.length > 0) {				var c = "";				$.map(r, function(e, r) {					c += "<p>" + e + "</p>";				});				errorTipsShow(c);			} else {				errorTipsHide();			}		}	});	$("#saveform").click(function() {		if (!$(this).parent().hasClass("ok")) {			return false;		}		if ($.sValid()) {			var pdr_amount = $.trim($('#pdr_amount').val());			var key = e;			if (!key) {				window.location.href = WapSiteUrl + "/tmpl/member/login.html";				return ;			}			var client = 'wap';			/* var r = $("#rc_sn").val();			var a = $.trim($("#captcha").val());			var c = $.trim($("#codekey").val()); */			$.request({				type: "post",				url: SYS.URL.pay.recharge,				data:{pdr_amount:pdr_amount,client:client},				dataType: "json",				success:function(result){					if(result.status==200){												location.href = 'rechargeinfo.html?pay_sn='+result.data.pay_sn;											}else{						 errorTipsShow("<p>"+result.msg+"<p>");					}				}			});		}	});});